#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

int main (int args, char** argv) {

    if (args >1) {
        std::cout << "Usage: Forfeit [script]" << std::endl;
    }
}